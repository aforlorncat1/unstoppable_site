<%= form_for(current_user.event_users.build(attended_event_id: @event_id)) do |f| %>
	<div><%= f.hidden_field :attended_event_id, :value => @event_id %></div>
	<%= f.submit "Attend", class: "btn btn-large btn primary" %>
<%end%>